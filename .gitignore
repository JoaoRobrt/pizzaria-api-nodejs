# Arquivo .gitignore para o Back-end (Node.js/Express com MongoDB/Mongoose)

### 1. Dependências e Compilação ###
# Pasta padrão de módulos do Node.js
/node_modules

# Logs npm
npm-debug.log
npm-debug.log.*
yarn-debug.log
yarn-error.log
lerna-debug.log

# Arquivos de build ou temporários
/dist
/build
/tmp

### 2. Variáveis de Ambiente e Chaves de Segurança (MUITA ATENÇÃO) ###
# O arquivo .env contém a MONGODB_URI, chaves de API, etc.
.env
.env.local
.env.*.local

# Chaves de Serviço do Firebase/JWT
# O arquivo com as credenciais do Firebase Admin SDK DEVE ser ignorado
*.key
*.pem
/serviceAccountKey.json 

# Certificados SSL/TLS
*.crt
*.cert

### 3. Sistema Operacional e Editores ###
# Mac OS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editores
.vscode/        # Arquivos de configuração do VS Code (opcional, se não quiser compartilhar configurações)
.idea/          # Arquivos de projeto do WebStorm/IntelliJ
*.swp
*~

### 4. Arquivos do MongoDB/Mongoose (se não for usar Atlas) ###
# Se estiver usando um MongoDB local, a pasta de dados deve ser ignorada
/data/db/
/config